# --- Backend API (Neon Postgres) - dùng khi chạy vercel dev hoặc deploy Vercel ---
# POSTGRES_URL= hoặc DATABASE_URL= (tự động khi thêm Postgres trong Vercel Dashboard)
# JWT_SECRET=O676LNGCEIyZ9m5h3uKgq4BaQ895HGYMP6OHESqSbJc=

# --- Frontend ---
# Base URL của backend API (tùy chọn). Để trống khi dùng API gộp trong project.
# VITE_API_URL= -> gọi /api cùng domain (mặc định)

# Proxy target khi chạy npm run dev với backend PHP riêng.
# VITE_PROXY_TARGET=http://127.0.0.1:8000

# Cloudflare Turnstile (captcha). Để trống = không load script.
# VITE_TURNSTILE_SITE_KEY=
