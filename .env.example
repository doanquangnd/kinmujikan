# Neon Postgres - tự động khi thêm Postgres trong Vercel Dashboard
# POSTGRES_URL=
# DATABASE_URL=

# JWT - bắt buộc đổi trong production (vd: openssl rand -base64 32)
# JWT_SECRET=

# Cloudflare Turnstile (captcha). Để trống = không load script.
# VITE_TURNSTILE_SITE_KEY=
